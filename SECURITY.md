# 安全说明

## 仓库访问

仓库必须保持 Private。GitHub 私有仓库不提供共享访问密码；访问权限通过 GitHub 账号邀请或组织 Team 管理。

推荐权限：

- IT 开发同学：`Write`，从分支提交 PR
- 只读评审者：`Read` 或 `Triage`
- 仓库所有者：`Admin`

不建议共享老板 GitHub 账号、Personal Access Token 或 SSH 私钥。

## 凭证处理

- 所有真实凭证只放在本地或服务器 `.env`/密钥管理服务中。
- `.env.example` 只能放变量名和无效占位值。
- 发现凭证进入 commit 后，不只删除文件，还必须立即轮换凭证并清理 Git 历史。

## 数据处理

- 员工周报、AI 分析、360 评分和互动日志属于公司内部敏感数据。
- 开发仓库只保留匿名 fixture。
- 生产问题截图或日志进入 Issue/PR 前必须脱敏。

## 漏洞反馈

请不要在公开渠道披露。通过公司内部飞书联系仓库所有者，说明影响范围、复现步骤和建议修复方式。

