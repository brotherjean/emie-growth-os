# 安全说明

## 公开仓库边界

本仓库公开的是**脱敏代码与演示数据**，不是成长 OS 生产环境的镜像。

- 公开仓库不包含真实员工名单、周报正文、AI 分析、互动日志或协同 360 明细。
- 公开仓库不包含生产 SQLite、Excel、通知 outbox、访问日志或服务器备份。
- 公开仓库不包含飞书 App Secret、Kimi API Key、token、SSH 私钥或其他凭据。
- `src/data/*.json` 只能保存虚构人员和匿名演示内容。

## 凭证处理

- 所有真实凭证只放在本地或服务器 `.env`、密钥管理服务中。
- `.env.example` 只能放变量名、公开端点和无效占位值。
- 发现凭证进入 commit 后，不只删除文件，还必须立即轮换凭证并清理 Git 历史。
- 不要在 Issue、PR、Actions 日志、终端截图或演示视频中暴露凭据。

## 数据处理

- 员工周报、AI 分析、360 评分和互动日志属于公司内部敏感数据。
- 开发测试使用虚构人员和匿名内容；复现生产问题时只保留最小结构。
- 生产数据导出不得作为 PR 附件，也不得提交到临时公开分支。
- 飞书真实写入、任务创建与消息发送必须区分 dry-run 和 production 模式。

## 权限与发布

- 公开可读不等于拥有生产权限；生产 SSO、数据库和飞书应用由独立配置控制。
- GitHub 合并不自动触发生产发布，生产发布需经过备份、构建、校验和人工 release gate。
- 新增权限、消息、任务、回调或外部访问能力时，PR 必须说明最小权限范围和回滚方式。

## 漏洞反馈

请不要在公开 Issue 中披露可利用细节、真实员工数据或凭据。请通过公司内部飞书联系仓库所有者，说明影响范围、复现步骤和建议修复方式；维护者确认安全后再公开修复说明。
